package be.vives.ti.bibuntitled.ui.game

import androidx.lifecycle.ViewModel
import be.vives.ti.bibuntitled.data.FirestoreRepository

class GameViewModel(private val firestoreRepository: FirestoreRepository) : ViewModel(){
    /*fun spelCodeExists(spelCode: String): Boolean{
        return firestoreRepository.spelCodeExists(spelCode, MainActivity())
    }*/
}